# Application name
spring.application.name=fortress-bank-service

# Application port
server.port=8081

# Environment profile
spring.profiles.active=prod

# Spring Cloud Config Server
spring.cloud.config.uri=http://localhost:8888

# Encrypt Key
encrypt.key=${ENCRYPT_KEY}