# Application Name
spring.application.name=fortress-auth-server

# Application Port
server.port=8901

# Environment profile
spring.profiles.active=dev

# Spring Cloud Config Server
spring.cloud.config.uri=http://localhost:8888

# Encrypt Key
encrypt.key=${ENCRYPT_KEY}

# Logging Level
logging.level.com.rzdp.fortressgateway=DEBUG
