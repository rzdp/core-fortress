# Application Name
spring.application.name=fortress-gateway

# Application Port
server.port=5555

# Environment profile
spring.profiles.active=dev

# Spring Cloud Config Server
spring.cloud.config.uri=http://localhost:8888

# Encrypt Key
encrypt.key=${ENCRYPT_KEY}

# Actuator Endpoints
management.endpoints.web.exposure.include=*

# Logging Level
logging.level.com.rzdp.fortressgateway=DEBUG
