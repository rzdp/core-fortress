# Application name
spring.application.name=fortress-member-service

# Application port
server.port=8080

# Environment profile
spring.profiles.active=dev

# Spring Cloud Config Server
spring.cloud.config.uri=http://localhost:8888

# Encrypt Key
encrypt.key=${ENCRYPT_KEY}